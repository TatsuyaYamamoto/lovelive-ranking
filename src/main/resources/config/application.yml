
spring:
  output:
    ansi:
      enabled: always
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lovelive_game_db?useSSL=false
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  servlet:
    context-path: /lovelive

app:
  credential:
    twitter-key:
    twitter-secret:
  allowed-origins:
    - http://localhost:8000
    - http://localhost:8080
    - http://games.sokontokoro-factory.net
    - https://games.sokontokoro-factory.net